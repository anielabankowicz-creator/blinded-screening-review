# Copy this file to .streamlit/secrets.toml locally or set these
# as deployment secrets in Streamlit Community Cloud.

SUPABASE_URL = "https://your-project-ref.supabase.co"
SUPABASE_ANON_KEY = "your-supabase-anon-key"

# Use a service-role key only in trusted server-side deployments.
# SUPABASE_SERVICE_ROLE_KEY = "your-service-role-key"
